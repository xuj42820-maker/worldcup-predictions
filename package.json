{
  "name": "worldcup-predictions",
  "version": "1.0.0",
  "description": "世界杯预测文档展示网站",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "deploy": "node build.js && git add . && git commit -m \"更新预测文档\" && git push"
  },
  "dependencies": {
    "mammoth": "^1.6.0"
  }
}
